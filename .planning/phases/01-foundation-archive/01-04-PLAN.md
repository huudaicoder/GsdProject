---
phase: 01-foundation
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - src/KhoThietBi.Application/Features/Categories/Commands/CreateCategoryCommand.cs
  - src/KhoThietBi.Application/Features/Categories/Commands/CreateCategoryCommandHandler.cs
  - src/KhoThietBi.Application/Features/Categories/Commands/CreateCategoryCommandValidator.cs
  - src/KhoThietBi.Application/Features/Categories/Commands/UpdateCategoryCommand.cs
  - src/KhoThietBi.Application/Features/Categories/Commands/UpdateCategoryCommandHandler.cs
  - src/KhoThietBi.Application/Features/Categories/Commands/UpdateCategoryCommandValidator.cs
  - src/KhoThietBi.Application/Features/Categories/Commands/DeleteCategoryCommand.cs
  - src/KhoThietBi.Application/Features/Categories/Commands/DeleteCategoryCommandHandler.cs
  - src/KhoThietBi.Application/Features/Categories/Queries/GetCategoriesQuery.cs
  - src/KhoThietBi.Application/Features/Categories/Queries/GetCategoriesQueryHandler.cs
  - src/KhoThietBi.Application/Features/Categories/DTOs/CategoryDto.cs
  - src/KhoThietBi.Application/Features/Categories/DTOs/CreateCategoryRequest.cs
  - src/KhoThietBi.Application/Features/Categories/DTOs/UpdateCategoryRequest.cs
  - src/KhoThietBi.API/Controllers/CategoriesController.cs
  - frontend/src/app/features/categories/categories.component.ts
  - frontend/src/app/features/categories/categories.component.html
  - frontend/src/app/features/categories/categories.component.scss
  - frontend/src/app/features/categories/category.service.ts
autonomous: true
requirements:
  - EQP-04
must_haves:
  truths:
    - "Admin can view a paginated list of categories at /categories"
    - "Admin can add a new category via modal dialog"
    - "Admin can edit an existing category via modal dialog"
    - "Admin can delete a category that has no linked equipment"
    - "Attempting to delete a category in use shows error toast 'Khong the xoa danh muc nay'"
    - "Search box filters categories by name"
  artifacts:
    - path: "src/KhoThietBi.API/Controllers/CategoriesController.cs"
      provides: "Category CRUD endpoints"
      exports: ["GET /api/categories", "POST /api/categories", "PUT /api/categories/{id}", "DELETE /api/categories/{id}"]
    - path: "frontend/src/app/features/categories/categories.component.ts"
      provides: "Categories table page with modal CRUD"
  key_links:
    - from: "CategoriesController"
      to: "GetCategoriesQueryHandler"
      via: "MediatR.Send"
      pattern: "ISender"
    - from: "categories.component.ts"
      to: "category.service.ts"
      via: "inject and call getCategories()"
      pattern: "CategoryService"
---

<objective>
Implement full CRUD for Categories: backend API endpoints with MediatR commands/queries, server-side pagination, search, delete protection for in-use records, and Angular frontend page with PrimeNG table + modal dialog.

Purpose: EQP-04 requires category management. Categories are FK targets for Equipment records in Phase 2.
Output: Working /categories page with server-side paginated table, add/edit modal, search, and delete with in-use protection.
</objective>

<execution_context>
@d:/MyGsd/.claude/get-shit-done/workflows/execute-plan.md
@d:/MyGsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/research/STACK.md

<interfaces>
<!-- From Plan 01 — Domain entities and DbContext -->
From src/KhoThietBi.Domain/Entities/Category.cs:
```csharp
public class Category : BaseEntity
{
    public string Name { get; set; }
    public string? Description { get; set; }
}
```

From src/KhoThietBi.Domain/Common/PagedResult.cs:
```csharp
public class PagedResult<T>
{
    public List<T> Items { get; set; }
    public int TotalCount { get; set; }
    public int Page { get; set; }
    public int PageSize { get; set; }
}
```

From src/KhoThietBi.Application/Common/Interfaces/IApplicationDbContext.cs:
```csharp
DbSet<Category> Categories { get; }
```

<!-- From Plan 01 — Frontend models -->
From frontend/src/app/core/models/api.models.ts:
```typescript
export interface PagedResult<T> {
  items: T[];
  totalCount: number;
  page: number;
  pageSize: number;
}
export interface PagedRequest {
  page: number;
  pageSize: number;
  sortBy?: string;
  sortDir?: 'asc' | 'desc';
  search?: string;
}
export interface Category {
  id: string;
  name: string;
  description?: string;
  createdAt: string;
  updatedAt?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Category backend CRUD with MediatR commands/queries</name>
  <files>
    src/KhoThietBi.Application/Features/Categories/Commands/CreateCategoryCommand.cs
    src/KhoThietBi.Application/Features/Categories/Commands/CreateCategoryCommandHandler.cs
    src/KhoThietBi.Application/Features/Categories/Commands/CreateCategoryCommandValidator.cs
    src/KhoThietBi.Application/Features/Categories/Commands/UpdateCategoryCommand.cs
    src/KhoThietBi.Application/Features/Categories/Commands/UpdateCategoryCommandHandler.cs
    src/KhoThietBi.Application/Features/Categories/Commands/UpdateCategoryCommandValidator.cs
    src/KhoThietBi.Application/Features/Categories/Commands/DeleteCategoryCommand.cs
    src/KhoThietBi.Application/Features/Categories/Commands/DeleteCategoryCommandHandler.cs
    src/KhoThietBi.Application/Features/Categories/Queries/GetCategoriesQuery.cs
    src/KhoThietBi.Application/Features/Categories/Queries/GetCategoriesQueryHandler.cs
    src/KhoThietBi.Application/Features/Categories/DTOs/CategoryDto.cs
    src/KhoThietBi.Application/Features/Categories/DTOs/CreateCategoryRequest.cs
    src/KhoThietBi.Application/Features/Categories/DTOs/UpdateCategoryRequest.cs
    src/KhoThietBi.API/Controllers/CategoriesController.cs
  </files>
  <read_first>
    src/KhoThietBi.Domain/Entities/Category.cs
    src/KhoThietBi.Domain/Common/PagedResult.cs
    src/KhoThietBi.Application/Common/Interfaces/IApplicationDbContext.cs
    src/KhoThietBi.Application/Common/Behaviors/ValidationBehavior.cs
    src/KhoThietBi.Infrastructure/Persistence/ApplicationDbContext.cs
    src/KhoThietBi.API/Controllers/AuthController.cs
    .planning/phases/01-foundation/01-CONTEXT.md
    .planning/phases/01-foundation/01-UI-SPEC.md
  </read_first>
  <action>
Build the complete CRUD backend for Categories per D-04 (feature-based folders), D-05 (MediatR), D-25 (separate DTOs), D-22 (block delete), D-27 (server-side pagination).

**Categories feature (Features/Categories/):**

1. **DTOs:**
   - `CategoryDto.cs`: record with `Guid Id`, `string Name`, `string? Description`, `DateTime CreatedAt`, `DateTime? UpdatedAt`
   - `CreateCategoryRequest.cs`: record with `string Name`, `string? Description`
   - `UpdateCategoryRequest.cs`: record with `string Name`, `string? Description`

2. **Queries:**
   - `GetCategoriesQuery.cs`: `record GetCategoriesQuery(int Page, int PageSize, string? Search, string? SortBy, string? SortDir) : IRequest<PagedResult<CategoryDto>>`
   - `GetCategoriesQueryHandler.cs`:
     - Inject `IApplicationDbContext`
     - Build query: `context.Categories.AsNoTracking()`
     - If `Search` is not null/empty: filter `.Where(c => c.Name.Contains(request.Search))` (case-insensitive via EF Core PostgreSQL)
     - Apply sorting: default sort by `Name` ascending. If `SortBy` is "name" or "createdAt", apply `OrderBy`/`OrderByDescending` based on `SortDir`
     - Get `totalCount` via `.CountAsync()`
     - Apply pagination: `.Skip((request.Page - 1) * request.PageSize).Take(request.PageSize)`
     - Project to `CategoryDto` using `.Select(c => new CategoryDto(c.Id, c.Name, c.Description, c.CreatedAt, c.UpdatedAt))`
     - Return `new PagedResult<CategoryDto> { Items = items, TotalCount = totalCount, Page = request.Page, PageSize = request.PageSize }`

3. **Commands:**
   - `CreateCategoryCommand.cs`: `record CreateCategoryCommand(string Name, string? Description) : IRequest<CategoryDto>`
   - `CreateCategoryCommandValidator.cs`: Name NotEmpty max 100 chars, Description max 500 chars. Error messages in Vietnamese: `"Ten danh muc khong duoc de trong."`, `"Ten danh muc khong duoc vuot qua 100 ky tu."`
   - `CreateCategoryCommandHandler.cs`: create new `Category`, add to `context.Categories`, save, return mapped `CategoryDto`

   - `UpdateCategoryCommand.cs`: `record UpdateCategoryCommand(Guid Id, string Name, string? Description) : IRequest<CategoryDto>`
   - `UpdateCategoryCommandValidator.cs`: same rules as create
   - `UpdateCategoryCommandHandler.cs`: find by Id, throw `NotFoundException` if null, update properties, save, return mapped `CategoryDto`

   - `DeleteCategoryCommand.cs`: `record DeleteCategoryCommand(Guid Id) : IRequest<Unit>`
   - `DeleteCategoryCommandHandler.cs`:
     - Find category by Id, throw `NotFoundException` if null
     - **Block delete check (D-22):** Check if any equipment references this category. In Phase 1, Equipment entity doesn't exist yet, so check `context.Set<Category>().Where(...)` — for now, the delete check will be a placeholder that always allows deletion. Add a `// TODO: Phase 2 — check Equipment FK here` comment. The check structure is: `var equipmentCount = await context.Equipment.CountAsync(e => e.CategoryId == request.Id)`. Since Equipment DbSet doesn't exist yet, wrap in a try/catch or conditionally check. **Simpler approach:** Add a method `HasLinkedEquipment(Guid categoryId)` that returns `false` for now. When Equipment entity is added in Phase 2, implement the real check.
     - If in use: throw a custom `ConflictException("Khong the xoa danh muc nay — dang duoc su dung boi X thiet bi.")`
     - If not in use: remove and save

4. **Controller (`CategoriesController.cs`):**
   ```csharp
   [ApiController]
   [Route("api/[controller]")]
   [Authorize(Roles = "Admin")]
   public class CategoriesController : ControllerBase
   {
       private readonly ISender _sender;
       public CategoriesController(ISender sender) => _sender = sender;

       [HttpGet]
       public async Task<ActionResult<PagedResult<CategoryDto>>> GetAll(
           [FromQuery] int page = 1,
           [FromQuery] int pageSize = 20,
           [FromQuery] string? search = null,
           [FromQuery] string? sortBy = null,
           [FromQuery] string? sortDir = null)
       {
           var result = await _sender.Send(new GetCategoriesQuery(page, pageSize, search, sortBy, sortDir));
           return Ok(result);
       }

       [HttpPost]
       public async Task<ActionResult<CategoryDto>> Create([FromBody] CreateCategoryRequest request)
       {
           var result = await _sender.Send(new CreateCategoryCommand(request.Name, request.Description));
           return CreatedAtAction(nameof(GetAll), new { id = result.Id }, result);
       }

       [HttpPut("{id:guid}")]
       public async Task<ActionResult<CategoryDto>> Update(Guid id, [FromBody] UpdateCategoryRequest request)
       {
           var result = await _sender.Send(new UpdateCategoryCommand(id, request.Name, request.Description));
           return Ok(result);
       }

       [HttpDelete("{id:guid}")]
       public async Task<IActionResult> Delete(Guid id)
       {
           await _sender.Send(new DeleteCategoryCommand(id));
           return NoContent();
       }
   }
   ```

**Exception types to add (if not already from Plan 02):**
- `NotFoundException` — returns 404 via ExceptionHandlingMiddleware
- `ConflictException` — returns 409 via ExceptionHandlingMiddleware

Update `ExceptionHandlingMiddleware` to handle `NotFoundException` -> 404 and `ConflictException` -> 409.
  </action>
  <verify>
    <automated>cd d:/MyGsd && dotnet build EquipmentManagement.sln 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - File `src/KhoThietBi.API/Controllers/CategoriesController.cs` contains `[Authorize(Roles = "Admin")]`
    - File `src/KhoThietBi.API/Controllers/CategoriesController.cs` contains `[HttpGet]` and `[HttpPost]` and `[HttpPut` and `[HttpDelete`
    - File `src/KhoThietBi.Application/Features/Categories/Queries/GetCategoriesQueryHandler.cs` contains `Skip` and `Take` and `CountAsync`
    - File `src/KhoThietBi.Application/Features/Categories/Commands/CreateCategoryCommandValidator.cs` contains `MaximumLength(100)` and `NotEmpty`
    - File `src/KhoThietBi.Application/Features/Categories/Commands/DeleteCategoryCommandHandler.cs` contains `ConflictException` or `Khong the xoa`
    - `dotnet build EquipmentManagement.sln` outputs `Build succeeded` with 0 errors
  </acceptance_criteria>
  <done>Category CRUD endpoints fully operational: GET with pagination+search+sort, POST create with validation, PUT update, DELETE with in-use protection. All use MediatR CQRS pattern with FluentValidation. Controller uses [Authorize(Roles = "Admin")].</done>
</task>

<task type="auto">
  <name>Task 2: Build Angular Categories page with PrimeNG table and modal dialog</name>
  <files>
    frontend/src/app/features/categories/categories.component.ts
    frontend/src/app/features/categories/categories.component.html
    frontend/src/app/features/categories/categories.component.scss
    frontend/src/app/features/categories/category.service.ts
  </files>
  <read_first>
    frontend/src/app/core/models/api.models.ts
    frontend/src/app/core/services/auth.service.ts
    frontend/src/app/app.routes.ts
    frontend/src/app/app.config.ts
    frontend/src/styles.scss
    .planning/phases/01-foundation/01-UI-SPEC.md
    .planning/phases/01-foundation/01-CONTEXT.md
  </read_first>
  <action>
Build the Categories Angular page per D-18 (table), D-19 (modal), D-20 (separate pages), D-21 (search), D-22 (block delete), D-27 (server-side pagination), and UI-SPEC Sections 4, 5, 6.

**Category Service** (`frontend/src/app/features/categories/category.service.ts`):
- Injectable `providedIn: 'root'`
- `getCategories(params: PagedRequest): Observable<PagedResult<Category>>` — GET `/api/categories` with query params
- `createCategory(data: { name: string; description?: string }): Observable<Category>` — POST `/api/categories`
- `updateCategory(id: string, data: { name: string; description?: string }): Observable<Category>` — PUT `/api/categories/${id}`
- `deleteCategory(id: string): Observable<void>` — DELETE `/api/categories/${id}`

**Categories Component** (`frontend/src/app/features/categories/`):

Replace the placeholder component created in Plan 03 with the full implementation.

Template structure per UI-SPEC Section 4:
```html
<!-- Page header zone -->
<div class="page-header">
  <h1>Danh muc thiet bi</h1>
  <button pButton label="Them moi" icon="pi pi-plus"
          (click)="openCreateDialog()"
          class="accent-btn"></button>
</div>

<!-- Search zone -->
<div class="search-zone">
  <p-inputGroup>
    <p-inputGroupAddon><i class="pi pi-search"></i></p-inputGroupAddon>
    <input pInputText placeholder="Tim kiem theo ten..."
           [(ngModel)]="searchTerm"
           (input)="onSearchChange()"
           style="width: 320px" />
  </p-inputGroup>
</div>

<!-- Table -->
<p-table [value]="categories()"
         [paginator]="true"
         [rows]="20"
         [totalRecords]="totalRecords()"
         [lazy]="true"
         (onLazyLoad)="onLazyLoad($event)"
         [loading]="isLoading()"
         [rowHover]="true"
         [showCurrentPageReport]="true"
         currentPageReportTemplate="{first} - {last} trong tong so {totalRecords} ban ghi">

  <ng-template pTemplate="header">
    <tr>
      <th style="width: 60px">#</th>
      <th pSortableColumn="name">Ten danh muc <p-sortIcon field="name"></p-sortIcon></th>
      <th>Mo ta</th>
      <th style="width: 120px">Thao tac</th>
    </tr>
  </ng-template>

  <ng-template pTemplate="body" let-category let-rowIndex="rowIndex">
    <tr>
      <td>{{ rowIndex + 1 + ((currentPage() - 1) * 20) }}</td>
      <td>{{ category.name }}</td>
      <td>{{ category.description }}</td>
      <td>
        <button pButton icon="pi pi-pencil" class="p-button-text p-button-sm"
                pTooltip="Sua" (click)="openEditDialog(category)"></button>
        <button pButton icon="pi pi-trash" class="p-button-text p-button-sm p-button-danger"
                pTooltip="Xoa" (click)="confirmDelete(category)"></button>
      </td>
    </tr>
  </ng-template>

  <ng-template pTemplate="emptymessage">
    <tr>
      <td colspan="4" class="text-center">
        <div class="empty-state">
          <h3>Chua co du lieu</h3>
          <p>Nhan "Them moi" de tao ban ghi dau tien.</p>
        </div>
      </td>
    </tr>
  </ng-template>

  <!-- Loading skeleton per UI-SPEC -->
  <ng-template pTemplate="loadingbody">
    <tr *ngFor="let _ of [1,2,3]">
      <td><p-skeleton></p-skeleton></td>
      <td><p-skeleton></p-skeleton></td>
      <td><p-skeleton></p-skeleton></td>
      <td><p-skeleton width="80px"></p-skeleton></td>
    </tr>
  </ng-template>
</p-table>

<!-- Add/Edit Modal Dialog per UI-SPEC Section 5 -->
<p-dialog [header]="isEditMode() ? 'Sua danh muc' : 'Them danh muc'"
          [(visible)]="dialogVisible"
          [modal]="true"
          [draggable]="false"
          [resizable]="false"
          [style]="{ width: '480px' }"
          [breakpoints]="{ '768px': '92vw' }">

  <form [formGroup]="categoryForm">
    <div class="field">
      <span class="p-float-label">
        <input pInputText id="name" formControlName="name" class="w-full" />
        <label for="name">Ten danh muc</label>
      </span>
      @if (categoryForm.get('name')?.touched && categoryForm.get('name')?.errors?.['required']) {
        <p-message severity="error" text="Ten danh muc khong duoc de trong."></p-message>
      }
      @if (categoryForm.get('name')?.errors?.['maxlength']) {
        <p-message severity="error" text="Ten danh muc khong duoc vuot qua 100 ky tu."></p-message>
      }
    </div>

    <div class="field">
      <span class="p-float-label">
        <textarea pInputTextarea id="description" formControlName="description"
                  class="w-full" rows="3"></textarea>
        <label for="description">Mo ta</label>
      </span>
    </div>
  </form>

  <ng-template pTemplate="footer">
    <button pButton label="Huy" class="p-button-outlined p-button-secondary"
            (click)="dialogVisible = false"></button>
    <button pButton [label]="'Luu danh muc'"
            (click)="saveCategory()"
            [loading]="isSaving()"
            [disabled]="isSaving()"></button>
  </ng-template>
</p-dialog>

<!-- Delete confirmation -->
<p-confirmDialog></p-confirmDialog>
<!-- Toast -->
<p-toast></p-toast>
```

Component class:
- Signals: `categories = signal<Category[]>([])`, `totalRecords = signal(0)`, `currentPage = signal(1)`, `isLoading = signal(false)`, `isSaving = signal(false)`, `isEditMode = signal(false)`, `selectedCategory = signal<Category | null>(null)`
- `dialogVisible = false`
- `searchTerm = ''` with debounce (300ms using `Subject` + `debounceTime`)
- `categoryForm = new FormGroup({ name: new FormControl('', [Validators.required, Validators.maxLength(100)]), description: new FormControl('', [Validators.maxLength(500)]) })`
- `onLazyLoad(event: TableLazyLoadEvent)`: extract page, pageSize, sortBy, sortDir from event, call `categoryService.getCategories(params)`, update signals
- `onSearchChange()`: debounce 300ms, then reload table from page 1 with search filter
- `openCreateDialog()`: reset form, set `isEditMode(false)`, open dialog
- `openEditDialog(category)`: patch form with category data, set `isEditMode(true)`, set `selectedCategory`, open dialog
- `saveCategory()`: if form invalid, mark all touched and return. If create mode: call `categoryService.createCategory(...)`. If edit mode: call `categoryService.updateCategory(selectedCategory.id, ...)`. On success: show success toast "Da luu thanh cong.", close dialog, reload table. On error: show error toast.
- `confirmDelete(category)`: use `ConfirmationService.confirm({ message: "Ban co chac chan muon xoa [category.name]? Thao tac nay khong the hoan tac.", header: "Xac nhan xoa", acceptLabel: "Xoa", rejectLabel: "Huy", ... })`. On accept: call `categoryService.deleteCategory(category.id)`. On success: show toast "Da xoa thanh cong.", reload table. **On 409 error (D-22):** show error toast with the message from API response (e.g., "Khong the xoa danh muc nay — dang duoc su dung boi X thiet bi.") with `life: 6000` (6 seconds per UI-SPEC).

SCSS:
```scss
.page-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 16px;

  h1 {
    font-size: 1.25rem;
    font-weight: 600;
    margin: 0;
  }
}
.accent-btn {
  background-color: var(--accent) !important;
  border-color: var(--accent) !important;
}
.search-zone {
  margin-bottom: 16px;
}
.empty-state {
  padding: 48px 0;
  h3 { font-weight: 600; margin: 0 0 8px; }
  p { color: var(--disabled-text); margin: 0; }
}
:host ::ng-deep {
  .p-datatable .p-datatable-tbody > tr:hover {
    background-color: var(--hover-row) !important;
  }
}
```
  </action>
  <verify>
    <automated>cd d:/MyGsd/frontend && npx ng build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/src/app/features/categories/categories.component.html` contains `p-table` and `p-dialog` and `p-confirmDialog`
    - File `frontend/src/app/features/categories/categories.component.html` contains `Them moi` and `Luu danh muc` and `Tim kiem theo ten`
    - File `frontend/src/app/features/categories/categories.component.html` contains `Chua co du lieu` (empty state)
    - File `frontend/src/app/features/categories/categories.component.ts` contains `onLazyLoad` and `confirmDelete` and `saveCategory`
    - File `frontend/src/app/features/categories/categories.component.ts` contains `debounceTime` or `debounce`
    - File `frontend/src/app/features/categories/category.service.ts` contains `/api/categories` and `getCategories` and `createCategory` and `deleteCategory`
    - `cd frontend && npx ng build` succeeds with 0 errors
  </acceptance_criteria>
  <done>Categories page at /categories with PrimeNG table (server-side pagination, lazy loading, row hover, skeleton loading, empty state), search box with 300ms debounce, add/edit modal dialog with floating labels and validation, delete with confirmation dialog and 409 error toast for in-use categories.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API CRUD endpoints | User input for category names and descriptions |
| Delete endpoint | Must validate referential integrity before deletion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Create/Update requests | mitigate | FluentValidation in MediatR pipeline validates all input (max length, required fields) before reaching handler. Angular form validation is UX only. |
| T-04-02 | Information Disclosure | GET pagination | mitigate | Controller uses `[Authorize(Roles = "Admin")]` — unauthenticated requests get 401. No data leakage without valid JWT with Admin role. |
| T-04-03 | Elevation of Privilege | Delete cascade | mitigate | Delete handler checks referential integrity before deletion. Return 409 Conflict with message when entity is in use (D-22). |
| T-04-04 | Injection | SQL via search parameter | mitigate | EF Core parameterizes all queries — the `search` query parameter is safely handled by LINQ `.Contains()` which generates parameterized SQL. No raw SQL used. |
</threat_model>

<verification>
1. `dotnet build EquipmentManagement.sln` succeeds
2. `cd frontend && npx ng build` succeeds
3. GET /api/categories returns paginated results
4. POST /api/categories with valid data returns 201
5. DELETE /api/categories/{id} returns 204 (or 409 if in use)
6. Categories page renders with table, modal, and search
</verification>

<success_criteria>
- Admin can view paginated category list at /categories (D-18, D-27)
- Admin can add a category via modal dialog (D-19)
- Admin can edit a category via modal dialog (D-19)
- Admin can delete a category (with in-use protection ready for Phase 2, D-22)
- Admin can search categories by name (D-21)
- All operations show success/error toasts with Vietnamese messages
- CategoriesController uses [Authorize(Roles = "Admin")] per CLAUDE.md
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
