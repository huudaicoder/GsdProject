---
phase: 02-reference-data-categories-suppliers-departments
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - kho-thiet-bi-ui/src/app/app.routes.ts
  - kho-thiet-bi-ui/src/app/shared/layout/sidebar/sidebar.component.ts
  - kho-thiet-bi-ui/src/app/features/categories/category.service.ts
  - kho-thiet-bi-ui/src/app/features/categories/categories.component.ts
  - kho-thiet-bi-ui/src/app/features/categories/categories.component.html
  - kho-thiet-bi-ui/src/app/features/categories/categories.component.scss
  - kho-thiet-bi-ui/src/app/features/categories/category-form.component.ts
  - kho-thiet-bi-ui/src/app/features/categories/category-form.component.html
  - kho-thiet-bi-ui/src/app/features/categories/category-form.component.scss
  - kho-thiet-bi-ui/src/app/features/suppliers/supplier.service.ts
  - kho-thiet-bi-ui/src/app/features/suppliers/suppliers.component.ts
  - kho-thiet-bi-ui/src/app/features/suppliers/suppliers.component.html
  - kho-thiet-bi-ui/src/app/features/suppliers/suppliers.component.scss
  - kho-thiet-bi-ui/src/app/features/suppliers/supplier-form.component.ts
  - kho-thiet-bi-ui/src/app/features/suppliers/supplier-form.component.html
  - kho-thiet-bi-ui/src/app/features/suppliers/supplier-form.component.scss
  - kho-thiet-bi-ui/src/app/features/departments/department.service.ts
  - kho-thiet-bi-ui/src/app/features/departments/departments.component.ts
  - kho-thiet-bi-ui/src/app/features/departments/departments.component.html
  - kho-thiet-bi-ui/src/app/features/departments/departments.component.scss
  - kho-thiet-bi-ui/src/app/features/departments/department-form.component.ts
  - kho-thiet-bi-ui/src/app/features/departments/department-form.component.html
  - kho-thiet-bi-ui/src/app/features/departments/department-form.component.scss
autonomous: false
requirements:
  - EQP-04
  - SUP-01
  - EMP-02

must_haves:
  truths:
    - "Admin navigates to /categories and sees a paginated table of equipment categories"
    - "Admin clicks 'Them moi' and a slide-over panel opens from the right at 30% viewport width"
    - "Admin fills Name and submits -> toast success and panel closes, new category appears in table with auto-generated code"
    - "Admin clicks 'Sua' on a row -> panel opens pre-filled, Code field is read-only"
    - "Admin clicks 'Xoa' -> confirm dialog appears, after confirm the row disappears"
    - "Admin clicks search button -> table refreshes with filtered results"
    - "Same behaviors work for /suppliers and /departments"
    - "Sidebar items for Danh muc thiet bi, Nha cung cap, Phong ban are no longer disabled"
  artifacts:
    - path: "kho-thiet-bi-ui/src/app/features/categories/categories.component.ts"
      provides: "Category list page with p-table and filter panel"
      contains: "CategoriesComponent"
    - path: "kho-thiet-bi-ui/src/app/features/categories/category-form.component.ts"
      provides: "Slide-over form for create/edit category"
      contains: "CategoryFormComponent"
    - path: "kho-thiet-bi-ui/src/app/features/categories/category.service.ts"
      provides: "HTTP service for category CRUD"
      contains: "CategoryService"
    - path: "kho-thiet-bi-ui/src/app/features/suppliers/suppliers.component.ts"
      provides: "Supplier list page"
      contains: "SuppliersComponent"
    - path: "kho-thiet-bi-ui/src/app/features/departments/departments.component.ts"
      provides: "Department list page"
      contains: "DepartmentsComponent"
    - path: "kho-thiet-bi-ui/src/app/app.routes.ts"
      provides: "Routes for /categories, /suppliers, /departments"
      contains: "loadComponent"
  key_links:
    - from: "categories.component.ts"
      to: "category.service.ts"
      via: "inject(CategoryService)"
      pattern: "inject\\(CategoryService\\)"
    - from: "category.service.ts"
      to: "/api/app/category"
      via: "HttpClient GET/POST/PUT/DELETE"
      pattern: "/api/app/category"
    - from: "app.routes.ts"
      to: "categories.component.ts"
      via: "loadComponent lazy import"
      pattern: "import.*features/categories"
    - from: "sidebar.component.ts"
      to: "/categories"
      via: "NavItem route with disabled: false"
      pattern: "disabled: false"
---

<objective>
Frontend: Angular 19 pages for all 3 reference data entities (categories, suppliers, departments) with PrimeNG components.

Purpose: Provide the admin UI to manage reference data per the user's design decisions. Each page has a 2-column layout (D-04): filter panel on the left with a search button (D-02), and a p-table on the right with server-side pagination (D-01). Create/edit uses a slide-over panel from the right (D-06, D-09). Delete uses a confirmation dialog (D-11) with a separate error dialog for blocked deletes (D-12).

Output: 3 feature pages at /categories, /suppliers, /departments — all functional and wired to Plan 01's backend APIs. Sidebar items enabled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-reference-data-categories-suppliers-departments/02-CONTEXT.md
@.planning/phases/02-reference-data-categories-suppliers-departments/02-RESEARCH.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/02-reference-data-categories-suppliers-departments/02-01-SUMMARY.md

<interfaces>
<!-- Existing codebase contracts the executor needs -->

From kho-thiet-bi-ui/src/app/app.routes.ts:
```typescript
import { Routes } from '@angular/router';
import { authGuard } from './core/guards/auth.guard';
export const routes: Routes = [
  { path: 'login', loadComponent: () => import('./features/auth/login/login.component').then(m => m.LoginComponent) },
  {
    path: '',
    loadComponent: () => import('./shared/layout/app-layout/app-layout.component').then(m => m.AppLayoutComponent),
    canActivate: [authGuard],
    children: [
      { path: 'dashboard', loadComponent: () => import('./features/dashboard/dashboard.component').then(m => m.DashboardComponent) },
      { path: '', redirectTo: 'dashboard', pathMatch: 'full' }
    ]
  },
  { path: '**', redirectTo: 'dashboard' }
];
```

From kho-thiet-bi-ui/src/app/shared/layout/sidebar/sidebar.component.ts:
```typescript
export interface NavItem { label: string; icon: string; route?: string; disabled: boolean; }
export interface NavGroup { label: string; icon: string; expanded: boolean; items: NavItem[]; }
// navGroups[0] = 'Danh muc' group with items:
//   { label: 'Danh muc thiet bi', icon: 'pi pi-tag', route: '/categories', disabled: true },
//   { label: 'Nha cung cap', icon: 'pi pi-building', route: '/suppliers', disabled: true },
//   { label: 'Phong ban', icon: 'pi pi-sitemap', route: '/departments', disabled: true },
//   ...
```

From kho-thiet-bi-ui/src/app/app.config.ts:
```typescript
providers: [
  provideZoneChangeDetection({ eventCoalescing: true }),
  provideRouter(routes),
  provideHttpClient(withInterceptors([jwtInterceptor, errorInterceptor])),
  provideAnimationsAsync(),
  providePrimeNG({ theme: { preset: Lara } }),
  MessageService,  // global MessageService for p-toast
]
```

From kho-thiet-bi-ui/src/app/core/interceptors/error.interceptor.ts:
```typescript
// Handles 401 (session expired toast + 3s redirect) and 403 (forbidden toast)
// Does NOT handle 400 errors — component-level error handling needed for delete-blocked
// Error shape from ABP: error.error.error.message contains the Vietnamese message
```

Backend API endpoints (from Plan 01):
- GET /api/app/category?filter=X&skipCount=0&maxResultCount=10 -> { items: CategoryDto[], totalCount: number }
- POST /api/app/category { name, description? } -> CategoryDto (with auto-generated code)
- PUT /api/app/category/{id} { name, description? } -> CategoryDto
- DELETE /api/app/category/{id} -> 204 or 400 (UserFriendlyException)
- Same pattern for /api/app/supplier and /api/app/department
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: All 3 feature pages (categories, suppliers, departments), routes, and sidebar activation</name>

  <files>
    kho-thiet-bi-ui/src/app/features/categories/category.service.ts
    kho-thiet-bi-ui/src/app/features/categories/categories.component.ts
    kho-thiet-bi-ui/src/app/features/categories/categories.component.html
    kho-thiet-bi-ui/src/app/features/categories/categories.component.scss
    kho-thiet-bi-ui/src/app/features/categories/category-form.component.ts
    kho-thiet-bi-ui/src/app/features/categories/category-form.component.html
    kho-thiet-bi-ui/src/app/features/categories/category-form.component.scss
    kho-thiet-bi-ui/src/app/features/suppliers/supplier.service.ts
    kho-thiet-bi-ui/src/app/features/suppliers/suppliers.component.ts
    kho-thiet-bi-ui/src/app/features/suppliers/suppliers.component.html
    kho-thiet-bi-ui/src/app/features/suppliers/suppliers.component.scss
    kho-thiet-bi-ui/src/app/features/suppliers/supplier-form.component.ts
    kho-thiet-bi-ui/src/app/features/suppliers/supplier-form.component.html
    kho-thiet-bi-ui/src/app/features/suppliers/supplier-form.component.scss
    kho-thiet-bi-ui/src/app/features/departments/department.service.ts
    kho-thiet-bi-ui/src/app/features/departments/departments.component.ts
    kho-thiet-bi-ui/src/app/features/departments/departments.component.html
    kho-thiet-bi-ui/src/app/features/departments/departments.component.scss
    kho-thiet-bi-ui/src/app/features/departments/department-form.component.ts
    kho-thiet-bi-ui/src/app/features/departments/department-form.component.html
    kho-thiet-bi-ui/src/app/features/departments/department-form.component.scss
    kho-thiet-bi-ui/src/app/app.routes.ts
    kho-thiet-bi-ui/src/app/shared/layout/sidebar/sidebar.component.ts
  </files>

  <read_first>
    kho-thiet-bi-ui/src/app/app.routes.ts
    kho-thiet-bi-ui/src/app/app.config.ts
    kho-thiet-bi-ui/src/app/shared/layout/sidebar/sidebar.component.ts
    kho-thiet-bi-ui/src/app/core/interceptors/error.interceptor.ts
    kho-thiet-bi-ui/src/app/core/interceptors/jwt.interceptor.ts
    kho-thiet-bi-ui/src/app/core/services/auth.service.ts
    kho-thiet-bi-ui/src/app/features/dashboard/dashboard.component.ts
    .planning/phases/02-reference-data-categories-suppliers-departments/02-RESEARCH.md
    .planning/phases/02-reference-data-categories-suppliers-departments/02-CONTEXT.md
  </read_first>

  <action>
Create ALL 3 feature modules (categories, suppliers, departments) plus routing and sidebar changes. All 3 entities follow the same UI pattern — implement categories first as the template, then replicate for suppliers and departments with entity-specific field differences.

**A) CategoryService (category.service.ts):**

```typescript
import { Injectable, inject } from '@angular/core';
import { HttpClient, HttpParams } from '@angular/common/http';
import { Observable } from 'rxjs';

export interface CategoryDto {
  id: string;
  code: string;
  name: string;
  description?: string;
}

export interface CreateUpdateCategoryDto {
  name: string;
  description?: string;
}

export interface PagedResult<T> {
  items: T[];
  totalCount: number;
}

@Injectable({ providedIn: 'root' })
export class CategoryService {
  private http = inject(HttpClient);
  private readonly BASE = '/api/app/category';

  getList(filter: string, skipCount: number, maxResultCount: number, sorting?: string): Observable<PagedResult<CategoryDto>> {
    let params = new HttpParams()
      .set('skipCount', skipCount)
      .set('maxResultCount', maxResultCount);
    if (filter) params = params.set('filter', filter);
    if (sorting) params = params.set('sorting', sorting);
    return this.http.get<PagedResult<CategoryDto>>(this.BASE, { params });
  }

  create(dto: CreateUpdateCategoryDto): Observable<CategoryDto> {
    return this.http.post<CategoryDto>(this.BASE, dto);
  }

  update(id: string, dto: CreateUpdateCategoryDto): Observable<CategoryDto> {
    return this.http.put<CategoryDto>(`${this.BASE}/${id}`, dto);
  }

  delete(id: string): Observable<void> {
    return this.http.delete<void>(`${this.BASE}/${id}`);
  }
}
```

**B) CategoriesComponent (categories.component.ts):**

Standalone component with these imports in the `@Component.imports` array:
`CommonModule, FormsModule, TableModule, ButtonModule, InputTextModule, ConfirmDialogModule, DialogModule, ToastModule, CategoryFormComponent`

Provide `[ConfirmationService]` in `providers` array (component-scoped, not root — per research pitfall #3).

Inject: `CategoryService`, `MessageService` (global from app.config), `ConfirmationService`.

Signal-based state:
- `categories = signal<CategoryDto[]>([]);`
- `totalCount = signal<number>(0);`
- `loading = signal<boolean>(false);`
- `filterText = signal<string>('');`
- `rows = signal<number>(10);`
- `first = signal<number>(0);`
- `panelVisible = signal<boolean>(false);`
- `editItem = signal<CategoryDto | null>(null);`
- `errorDialogVisible = signal<boolean>(false);`
- `errorDialogMessage = signal<string>('');`

Methods:
- `ngOnInit()`: call `loadData()`
- `loadData()`: set loading true, call `categoryService.getList(filterText(), first(), rows())`, on success set categories, totalCount, loading false
- `onSearch()`: per D-02, reset first to 0, call loadData() — triggered by search BUTTON click, not input change
- `onLazyLoad(event: TableLazyLoadEvent)`: set first and rows from event, call loadData() — handles pagination and sorting
- `openCreate()`: set editItem null, panelVisible true
- `openEdit(item: CategoryDto)`: set editItem to item, panelVisible true
- `onSaved()`: per D-07, set panelVisible false, show success toast ("Thanh cong" / "Luu danh muc thanh cong."), call loadData()
- `confirmDelete(item: CategoryDto)`: per D-11, call confirmationService.confirm with message "Ban co chac muon xoa \"{item.name}\" khong?", header "Xac nhan xoa", acceptLabel "Xac nhan", rejectLabel "Huy"
- `deleteItem(item: CategoryDto)`: call categoryService.delete, on success show toast + loadData(), on error per D-12: read `err?.error?.error?.message` (ABP UserFriendlyException format), set errorDialogMessage and errorDialogVisible to true

**C) CategoriesComponent template (categories.component.html):**

Per D-04, 2-column layout:

```html
<p-toast />
<p-confirmDialog />

<!-- Error dialog for delete-blocked (D-12) -->
<p-dialog header="Khong the xoa" [(visible)]="errorDialogVisible" [modal]="true" [closable]="true" [style]="{ 'min-width': '400px' }">
  <p>{{ errorDialogMessage() }}</p>
  <ng-template #footer>
    <p-button label="Dong" (onClick)="errorDialogVisible.set(false)" />
  </ng-template>
</p-dialog>

<div class="page-container">
  <!-- Left: Filter panel -->
  <div class="filter-panel">
    <h3>Tim kiem</h3>
    <div class="filter-field">
      <input pInputText
             type="text"
             placeholder="Nhap ten hoac ma..."
             [ngModel]="filterText()"
             (ngModelChange)="filterText.set($event)"
             (keydown.enter)="onSearch()"
             class="w-full" />
    </div>
    <p-button label="Tim kiem" icon="pi pi-search" (onClick)="onSearch()" styleClass="w-full" />
  </div>

  <!-- Right: Data table -->
  <div class="table-panel">
    <div class="table-header">
      <h2>Danh muc thiet bi</h2>
      <p-button label="Them moi" icon="pi pi-plus" (onClick)="openCreate()" />
    </div>

    <p-table
      [value]="categories()"
      [lazy]="true"
      [paginator]="true"
      [rows]="rows()"
      [totalRecords]="totalCount()"
      [loading]="loading()"
      [rowsPerPageOptions]="[10, 20, 50]"
      (onLazyLoad)="onLazyLoad($event)"
      [first]="first()"
      [showCurrentPageReport]="true"
      currentPageReportTemplate="Hien thi {first} den {last} trong {totalRecords} ban ghi"
      [tableStyle]="{ 'min-width': '50rem' }">

      <ng-template #header>
        <tr>
          <th pSortableColumn="code" style="width: 120px">Ma <p-sortIcon field="code" /></th>
          <th pSortableColumn="name">Ten <p-sortIcon field="name" /></th>
          <th>Mo ta</th>
          <th style="width: 150px">Thao tac</th>
        </tr>
      </ng-template>

      <ng-template #body let-item>
        <tr>
          <td>{{ item.code }}</td>
          <td>{{ item.name }}</td>
          <td>{{ item.description }}</td>
          <td>
            <p-button icon="pi pi-pencil" [text]="true" [rounded]="true" severity="info" (onClick)="openEdit(item)" pTooltip="Sua" />
            <p-button icon="pi pi-trash" [text]="true" [rounded]="true" severity="danger" (onClick)="confirmDelete(item)" pTooltip="Xoa" />
          </td>
        </tr>
      </ng-template>

      <ng-template #emptymessage>
        <tr>
          <td colspan="4" class="text-center p-4">Khong co du lieu.</td>
        </tr>
      </ng-template>
    </p-table>
  </div>
</div>

<!-- Slide-over form panel -->
<app-category-form
  [(visible)]="panelVisible"
  [editItem]="editItem()"
  (saved)="onSaved()" />
```

**D) CategoriesComponent styles (categories.component.scss):**

```scss
.page-container {
  display: flex;
  gap: 1.5rem;
  padding: 1.5rem;
}

.filter-panel {
  width: 280px;
  flex-shrink: 0;

  h3 {
    margin-top: 0;
    margin-bottom: 1rem;
    font-size: 1rem;
    font-weight: 600;
  }

  .filter-field {
    margin-bottom: 1rem;
  }
}

.table-panel {
  flex: 1;
  min-width: 0;
}

.table-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 1rem;

  h2 {
    margin: 0;
    font-size: 1.25rem;
    font-weight: 600;
  }
}
```

**E) CategoryFormComponent (category-form.component.ts):**

Standalone component with imports: `CommonModule, ReactiveFormsModule, DrawerModule (primeng/drawer), InputTextModule, TextareaModule, ButtonModule`

IMPORTANT: In PrimeNG 19, the sidebar component has been renamed to `Drawer`. Use `import { DrawerModule } from 'primeng/drawer';` and the template tag `<p-drawer>` instead of `<p-sidebar>`. If the PrimeNG 19 version installed does NOT have DrawerModule, fall back to `import { SidebarModule } from 'primeng/sidebar';` and `<p-sidebar>`.

Check which is available by reading `node_modules/primeng/drawer/` or `node_modules/primeng/sidebar/`. Use whichever exists.

Input/Output:
- `@Input() visible: boolean = false;`
- `@Input() editItem: CategoryDto | null = null;`
- `@Output() visibleChange = new EventEmitter<boolean>();`
- `@Output() saved = new EventEmitter<void>();`

Form (ReactiveFormsModule):
```typescript
form = this.fb.group({
  name: ['', [Validators.required, Validators.maxLength(100)]],
  description: ['', Validators.maxLength(500)]
});
```

Property: `saving = false;`
Computed: `get isEdit(): boolean { return !!this.editItem; }`

`ngOnChanges()`: when visible becomes true, if editItem set patchValue, else reset form.

`onHide()`: emit visibleChange(false).

`onSubmit()`: if invalid markAllAsTouched and return. Set saving true. Build `CreateUpdateCategoryDto`. Call create or update based on isEdit. On success: saving false, emit saved. On error: saving false (error interceptor handles 401/403; 400 validation errors appear in component — read `err.error?.error?.validationErrors` and set form field errors if present).

**F) CategoryFormComponent template (category-form.component.html):**

Use `<p-drawer>` (or `<p-sidebar>`) per D-06, D-09:

```html
<p-drawer
  [visible]="visible"
  (visibleChange)="onHide()"
  position="right"
  [style]="{ width: '30vw' }"
  [closeOnEscape]="true"
  [header]="isEdit ? 'Sua danh muc' : 'Them danh muc moi'">

  <form [formGroup]="form" (ngSubmit)="onSubmit()">
    <!-- Code field (read-only, shown only in edit mode) -->
    @if (isEdit) {
      <div class="field mb-4">
        <label class="block mb-1 font-semibold">Ma</label>
        <input pInputText [value]="editItem?.code ?? ''" [disabled]="true" class="w-full" />
      </div>
    }

    <!-- Name field (required) per D-10 -->
    <div class="field mb-4">
      <label class="block mb-1 font-semibold">Ten <span class="text-red-500">*</span></label>
      <input pInputText formControlName="name" class="w-full" />
      <!-- D-08: inline error under field -->
      @if (form.controls.name.invalid && form.controls.name.touched) {
        <small class="text-red-500 block mt-1">
          @if (form.controls.name.errors?.['required']) {
            Ten danh muc khong duoc de trong.
          } @else if (form.controls.name.errors?.['maxlength']) {
            Ten khong duoc vuot qua 100 ky tu.
          }
        </small>
      }
    </div>

    <!-- Description field (optional) per D-10 -->
    <div class="field mb-4">
      <label class="block mb-1 font-semibold">Mo ta</label>
      <textarea pTextarea formControlName="description" [rows]="4" class="w-full"></textarea>
      @if (form.controls.description.invalid && form.controls.description.touched) {
        <small class="text-red-500 block mt-1">Mo ta khong duoc vuot qua 500 ky tu.</small>
      }
    </div>

    <div class="flex gap-2 justify-end">
      <p-button label="Huy" severity="secondary" [outlined]="true" (onClick)="onHide()" />
      <p-button label="Luu" type="submit" [loading]="saving" />
    </div>
  </form>
</p-drawer>
```

**G) CategoryFormComponent styles (category-form.component.scss):**

```scss
.field label {
  font-size: 0.875rem;
  color: #374151;
}
```

**H) Suppliers — Replicate category pattern with these differences:**

`SupplierService`:
- BASE = `/api/app/supplier`
- `SupplierDto`: id, code, name, address?, email?, phone?
- `CreateUpdateSupplierDto`: name, address?, email?, phone?

`SuppliersComponent`:
- Same 2-column layout as CategoriesComponent
- Table columns per D-03: Ma | Ten | Mo ta (use address as description column) | Thao tac
  IMPORTANT: Supplier does not have a "Description" field. Instead show Address in the third column. Column header should be "Dia chi" not "Mo ta".
  Actual columns: Ma | Ten | Dia chi | Thao tac
- Page title: "Nha cung cap"
- Toast messages reference "nha cung cap" instead of "danh muc"

`SupplierFormComponent`:
- Form fields per D-10: name (required), address (optional), email (optional, validate email format client-side), phone (optional)
- Form group:
  ```typescript
  form = this.fb.group({
    name: ['', [Validators.required, Validators.maxLength(200)]],
    address: ['', Validators.maxLength(500)],
    email: ['', [Validators.maxLength(100), Validators.email]],
    phone: ['', Validators.maxLength(20)]
  });
  ```
- Template: 4 input fields (Name, Dia chi, Email, So dien thoai) + code read-only in edit mode
- Panel header: "Sua nha cung cap" / "Them nha cung cap moi"

**I) Departments — Replicate category pattern with these differences:**

`DepartmentService`:
- BASE = `/api/app/department`
- `DepartmentDto`: id, code, name
- `CreateUpdateDepartmentDto`: name

`DepartmentsComponent`:
- Table columns: Ma | Ten | Thao tac (only 3 columns — Department has no description/address)
- Page title: "Phong ban"
- Toast messages reference "phong ban"
- Empty message colspan = 3 (not 4)

`DepartmentFormComponent`:
- Form fields per D-10: name (required) only
- Form group: `form = this.fb.group({ name: ['', [Validators.required, Validators.maxLength(100)]] });`
- Template: single Name field + code read-only in edit mode
- Panel header: "Sua phong ban" / "Them phong ban moi"

**J) Routes (app.routes.ts) — Modify existing file:**

Add 3 child routes inside the existing `children` array (the one under `canActivate: [authGuard]`), BEFORE the `{ path: '', redirectTo: 'dashboard', pathMatch: 'full' }` entry:

```typescript
{
  path: 'categories',
  loadComponent: () => import('./features/categories/categories.component').then(m => m.CategoriesComponent)
},
{
  path: 'suppliers',
  loadComponent: () => import('./features/suppliers/suppliers.component').then(m => m.SuppliersComponent)
},
{
  path: 'departments',
  loadComponent: () => import('./features/departments/departments.component').then(m => m.DepartmentsComponent)
},
```

**K) Sidebar (sidebar.component.ts) — Modify existing file:**

In the `navGroups` array, first group ('Danh muc'), change `disabled: true` to `disabled: false` for these 3 items:
- `{ label: 'Danh muc thiet bi', icon: 'pi pi-tag', route: '/categories', disabled: false }`
- `{ label: 'Nha cung cap', icon: 'pi pi-building', route: '/suppliers', disabled: false }`
- `{ label: 'Phong ban', icon: 'pi pi-sitemap', route: '/departments', disabled: false }`

All other items in all groups remain `disabled: true`.

**IMPORTANT PrimeNG 19 NOTES:**
- For `p-table` lazy loading, import `TableModule` from `primeng/table` and `TableLazyLoadEvent` from `primeng/table`
- For `p-drawer`/`p-sidebar`, check which module exists in PrimeNG 19.1.4. The API may use `DrawerModule` from `primeng/drawer`. If not available, use `SidebarModule` from `primeng/sidebar`.
- For `p-dialog`, import `DialogModule` from `primeng/dialog`
- For `p-confirmDialog`, import `ConfirmDialogModule` from `primeng/confirmdialog`
- For `p-toast`, import `ToastModule` from `primeng/toast`
- For `p-button`, import `ButtonModule` from `primeng/button`
- For `pInputText`, import `InputTextModule` from `primeng/inputtext`
- For `pTextarea`, import `TextareaModule` from `primeng/textarea`
- For `pTooltip`, import `TooltipModule` from `primeng/tooltip`
- Template syntax: Use Angular 19 `@if` / `@for` control flow (not *ngIf/*ngFor)
- PrimeNG 19 template refs: use `#header`, `#body`, `#emptymessage`, `#footer` (not `pTemplate="header"`)
  </action>

  <verify>
    <automated>cd kho-thiet-bi-ui && npx ng build --configuration=development 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - File `kho-thiet-bi-ui/src/app/features/categories/categories.component.ts` exists and contains `export class CategoriesComponent`
    - File `kho-thiet-bi-ui/src/app/features/categories/category-form.component.ts` exists and contains `export class CategoryFormComponent`
    - File `kho-thiet-bi-ui/src/app/features/categories/category.service.ts` exists and contains `/api/app/category`
    - File `kho-thiet-bi-ui/src/app/features/suppliers/suppliers.component.ts` exists and contains `export class SuppliersComponent`
    - File `kho-thiet-bi-ui/src/app/features/suppliers/supplier.service.ts` exists and contains `/api/app/supplier`
    - File `kho-thiet-bi-ui/src/app/features/departments/departments.component.ts` exists and contains `export class DepartmentsComponent`
    - File `kho-thiet-bi-ui/src/app/features/departments/department.service.ts` exists and contains `/api/app/department`
    - `app.routes.ts` contains `path: 'categories'` and `path: 'suppliers'` and `path: 'departments'` with `loadComponent`
    - `sidebar.component.ts` contains `route: '/categories', disabled: false` and `route: '/suppliers', disabled: false` and `route: '/departments', disabled: false`
    - `categories.component.html` contains `p-table` and `p-confirmDialog` and `p-dialog` (for D-12 error dialog)
    - `categories.component.html` contains `p-drawer` or `p-sidebar` (for D-06 slide-over panel)
    - `category-form.component.html` contains `formControlName="name"` and `formControlName="description"`
    - `supplier-form.component.html` contains `formControlName="email"` and `formControlName="phone"`
    - `categories.component.html` contains `[rowsPerPageOptions]="[10, 20, 50]"` (per D-01)
    - `categories.component.ts` contains `onSearch()` method (per D-02: button trigger)
    - `npx ng build --configuration=development` passes with 0 errors
  </acceptance_criteria>

  <done>
All 3 feature pages built and building cleanly. Categories page has 2-column layout with filter panel + search button + p-table with server-side pagination. Slide-over form panel opens from right at 30% width. Delete confirmation dialog and error dialog implemented. Same pattern replicated for suppliers (with address/email/phone fields) and departments (with name-only field). Routes registered and sidebar items enabled.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of all 3 reference data pages</name>

  <files>
    kho-thiet-bi-ui/src/app/features/categories/categories.component.ts
    kho-thiet-bi-ui/src/app/features/suppliers/suppliers.component.ts
    kho-thiet-bi-ui/src/app/features/departments/departments.component.ts
  </files>

  <action>
Human visual and functional verification of the 3 reference data pages built in Task 1. Start backend and frontend servers, log in as admin, and verify all CRUD operations, search, pagination, slide-over panels, and delete confirmation dialogs work as designed per D-01 through D-12.
  </action>

  <what-built>
Complete CRUD UI for Categories (/categories), Suppliers (/suppliers), and Departments (/departments). Each page has a 2-column layout with filter panel on the left and data table on the right. Create/edit uses a slide-over panel from the right. Delete has a confirmation dialog. All connected to the backend API from Plan 01.
  </what-built>

  <how-to-verify>
Prerequisites: Backend running (`dotnet run` in KhoThietBi.HttpApi.Host), frontend running (`npx ng serve` in kho-thiet-bi-ui). Login as admin/1q2w3E*.

1. **Sidebar navigation:**
   - Verify "Danh muc thiet bi", "Nha cung cap", "Phong ban" are clickable (not grayed out)
   - Click each one and verify it navigates to the correct page

2. **Categories page (/categories):**
   - Verify 2-column layout: filter panel on left, table on right
   - Click "Them moi" -> verify slide-over panel opens from the RIGHT at ~30% viewport width
   - Fill in Name "IT Equipment", Description "Thiet bi CNTT" -> click "Luu"
   - Verify toast success message appears and panel auto-closes
   - Verify new row appears in table with auto-generated code (DM-001)
   - Click the edit (pencil) icon on the row -> verify panel opens with pre-filled values, Code field is read-only
   - Change name -> save -> verify table updates
   - Click the delete (trash) icon -> verify confirm dialog appears with "Ban co chac muon xoa...?"
   - Click "Xac nhan" -> verify row disappears

3. **Search (D-02):**
   - Create 2 categories: "Alpha" and "Beta"
   - Type "Alpha" in the search field
   - Click "Tim kiem" button -> verify only "Alpha" row appears
   - Clear search, click "Tim kiem" -> verify both appear

4. **Pagination (D-01):**
   - If enough records exist (>10), verify page navigation works
   - Verify rows-per-page dropdown shows 10, 20, 50

5. **Suppliers page (/suppliers):**
   - Same create/edit/delete/search flow as categories
   - Verify form has: Name, Dia chi, Email, So dien thoai fields
   - Create a supplier with email and phone -> verify all fields saved correctly

6. **Departments page (/departments):**
   - Same create/edit/delete/search flow
   - Verify form has only Name field (simpler than categories)
   - Table should have 3 columns: Ma, Ten, Thao tac (no description column)

7. **Error dialog (D-12):**
   - This cannot be fully tested in Phase 2 (no Equipment/Employee entities yet to create FK references)
   - Verify the error dialog component EXISTS in the template (visible when errorDialogVisible is true)
  </how-to-verify>

  <verify>
    <automated>cd kho-thiet-bi-ui && npx ng build --configuration=development 2>&1 | tail -5</automated>
  </verify>

  <done>
Human has verified all 3 reference data pages visually and functionally: CRUD works, search filters correctly, pagination shows 10/20/50 options, slide-over panels open from right at 30% width, delete confirmation dialogs appear, sidebar items are enabled and navigable. User typed "approved".
  </done>

  <resume-signal>Type "approved" if all verifications pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User browser -> Angular SPA | User interaction, client-side input |
| Angular SPA -> ABP API | HTTP calls with JWT Bearer token (via interceptor) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Spoofing | Angular route access | mitigate | `authGuard` (CanActivateFn) protects all child routes including /categories, /suppliers, /departments. Unauthenticated users redirected to /login. |
| T-02-10 | Tampering | Client-side form data | mitigate | Reactive Forms with `Validators.required` and `Validators.maxLength` provide client-side validation. Server-side FluentValidation (Plan 01) is the authoritative check. Client validation is UX only. |
| T-02-11 | Tampering | XSS via category name/description display | mitigate | Angular template binding `{{ item.name }}` auto-escapes HTML. No `[innerHTML]` usage. PrimeNG p-table uses text rendering by default. |
| T-02-12 | Information Disclosure | ABP error message in delete dialog | accept | Error messages from `UserFriendlyException` are intentionally shown to admin. Messages contain only entity counts, no sensitive data. Admin-only system. |
| T-02-13 | Tampering | JWT token in localStorage | accept | Phase 1 design decision. localStorage is accessible to XSS but Angular's built-in sanitization + no [innerHTML] usage mitigates. Admin-only internal tool — risk accepted per Phase 1 threat model. |
</threat_model>

<verification>
1. `npx ng build --configuration=development` in kho-thiet-bi-ui — 0 errors
2. Visual verification: all 3 pages render with correct layout per D-04
3. Functional verification: CRUD operations work for all 3 entities
4. Sidebar items for Phase 2 routes are enabled (disabled: false)
5. Search button triggers API call with filter parameter (D-02)
6. Slide-over panel opens from right at 30% viewport (D-06, D-09)
7. Delete confirmation dialog appears before delete action (D-11)
</verification>

<success_criteria>
- 3 feature pages (categories, suppliers, departments) at their respective routes, each with:
  - 2-column layout: filter panel (left) + data table (right) per D-04
  - PrimeNG p-table with server-side pagination (10/20/50) per D-01
  - Search button trigger (not real-time) per D-02
  - Table columns: Ma, Ten, [entity-specific], Thao tac per D-03
  - Slide-over panel from right at 30% viewport for create/edit per D-06, D-09
  - Toast on success + auto-close panel per D-07
  - Inline validation errors under fields per D-08
  - Confirm dialog before delete per D-11
  - Error dialog (not toast) for blocked deletes per D-12
- Routes registered in app.routes.ts with lazy loading
- Sidebar items enabled (disabled: false) for the 3 routes
- `ng build` passes with 0 errors
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/02-reference-data-categories-suppliers-departments/02-02-SUMMARY.md`
</output>
